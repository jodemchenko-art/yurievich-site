/**
 * Отзывы.
 *
 * ⚠️ Здесь ПУСТО намеренно (очищено 2026-07-26 в ходе SEO-аудита).
 * Раньше лежали 7 именных «отзывов» с городами, объектами и суммами, которых
 * никто из клиентов не писал. Они уходили в Schema.org как настоящие Review —
 * это недостоверная разметка (риск для доверия и в Яндексе, и в Google),
 * плюс Google всё равно не показывает рейтинг компании о самой себе.
 *
 * Как наполнять правильно:
 * 1. Берём отзыв с площадки, где он опубликован (Авито / Я.Карты / 2ГИС).
 * 2. Обязательно храним `sourceUrl` — прямую ссылку на этот отзыв.
 * 3. Без ссылки-пруфа отзыв на сайт НЕ добавляем и в разметку НЕ отдаём.
 */

export type Review = {
  id: string;
  author: string;
  authorCity?: string;
  date: string;
  rating: 1 | 2 | 3 | 4 | 5;
  body: string;
  project?: string;
  source: 'Авито' | 'Яндекс.Карты' | 'Сайт' | '2ГИС';
  /** Прямая ссылка на отзыв на площадке. Без неё отзыв не публикуем. */
  sourceUrl: string;
};

export const REVIEWS: Review[] = [];

export const AGGREGATE_RATING = {
  ratingValue: '5.0',
  reviewCount: 35,
  bestRating: '5',
  worstRating: '1',
};
