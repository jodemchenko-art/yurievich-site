import type { Config } from 'tailwindcss';

/**
 * ПАЛИТРА «СИНЬКА И БУМАГА» (26.07.2026, вторая итерация).
 *
 * Первая версия была тёмно-синяя с янтарём — заказчик справедливо назвал это
 * «чёрно-жёлтым дефолтом». Разбор показал, что дело не в оттенке: шаблонной
 * страницу делает СТРУКТУРА «тёмный фон + одно яркое пятно», в какой бы цвет
 * это пятно ни красили. Поэтому здесь перевёрнута сама схема:
 *
 *   • светлая бумага — 70% высоты страницы, тёмного ровно три узкие полосы;
 *   • тёмное — ТЁПЛЫЙ графит-битум (R>G>B), а не сине-чёрный «ночной режим»;
 *   • единственный хроматический цвет — ультрамарин берлинской синьки,
 *     тот самый пигмент, которым печатали чертежи. Он не жёлтый, не бирюзовый
 *     (второй дефолт) и не красный (запрещён владельцем);
 *   • второй план — песок опалубки: он снимает монохромность, ради которой
 *     обычно тащат второй яркий цвет.
 *
 * Правило приёмки простое: закрой ладонью кнопку заявки — синего пятна
 * крупнее точки на экране остаться не должно.
 *
 * Две светлые плоскости РАЗНОЙ ТЕМПЕРАТУРЫ: тёплая известь «объясняем»
 * (разрез, этапы, FAQ) и холодная «считаем» (цены, расчёт). Встык они не
 * ставятся никогда — между ними либо тёмная секция, либо paper0 как буфер.
 */
const config: Config = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // === Сквозные токены старых страниц (блог, гео, словарь) ===
        // Имена оставлены, значения переведены на новую палитру — иначе
        // главная и остальные 150 страниц разъедутся по цвету.
        brand: {
          red: '#1E44AE',        // историческое имя; физически — ультрамарин
          'red-dark': '#17357F',
          accent: '#1E44AE',
          'accent-dark': '#17357F',
          ink: '#191713',
          mute: '#5A554D',
          sand: '#F0EDE6',
          line: '#DCD5C8',
        },

        // === Тёмные плоскости: тёплый графит, не сине-чёрный ===
        bp: {
          950: '#1A1714', // hero, форма заявки, футер
          900: '#24211C', // объекты
          800: '#2F2B25', // карточки ВНУТРИ тёмной секции
          700: '#2F2B25',
          line: '#322D26', // декоративная линия на тёмном
          text: '#B3ABA0', // вторичный текст на тёмном
        },

        // === Акцент: ультрамарин. Только кнопка, активное, фокус, ссылка ===
        signal: {
          DEFAULT: '#1E44AE',
          dark: '#17357F',
          soft: '#A8BEF7', // подсветка на тёмном
          bright: '#4C7CF0', // активное состояние на графите
        },

        // === Светлые плоскости ===
        paper0: '#FBFAF7',  // карточки, поля форм, чистый лист
        paper: '#F0EDE6',   // тёплая известь — база сайта
        paper2: '#E2DCD0',  // песчаник: зебра, плашки
        cold: '#E9ECEE',    // холодный «лист замера»: цены и расчёт
        cold2: '#DBE1E4',

        // === Линии: декоративные и несущие — это разные токены ===
        hair: '#DCD5C8',    // миллиметровка и мягкие разделители
        hairc: '#CFD6D9',   // то же на холодной плоскости
        rule: '#7A7265',    // несущие: рамки карточек, разлиновка, инпуты
        rulec: '#77828A',   // несущие на холодной
        ruled: '#746A5C',   // несущие на графите

        // === Текст ===
        graphite: '#191713',  // основной на светлом
        inkmute: '#5A554D',   // вторичный на тёплом светлом
        inkcold: '#545A5E',   // вторичный на холодном светлом
        chalk: '#F2EFE9',     // основной на тёмном
        chalkdim: '#B3ABA0',  // вторичный на тёмном

        // === Материальный второй план ===
        sand: '#C4B7A4',
      },
      fontFamily: {
        sans: ['Manrope', 'Inter', 'system-ui', 'sans-serif'],
        mono: ['JetBrains Mono', 'ui-monospace', 'SFMono-Regular', 'Menlo', 'Consolas', 'monospace'],
      },
      maxWidth: {
        content: '1200px',
      },
      animation: {
        'fade-in': 'fadeIn 0.6s ease-out',
        'fade-up': 'fadeUp 0.6s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        fadeUp: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
      },
    },
  },
  plugins: [],
};

export default config;
