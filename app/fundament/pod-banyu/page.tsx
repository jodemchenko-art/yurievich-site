import TypePage, { typeMetadata } from '@/components/foundation/TypePage';

export const metadata = typeMetadata('pod-banyu');

export default function Page() {
  return <TypePage slug="pod-banyu" />;
}
