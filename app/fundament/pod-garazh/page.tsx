import TypePage, { typeMetadata } from '@/components/foundation/TypePage';

export const metadata = typeMetadata('pod-garazh');

export default function Page() {
  return <TypePage slug="pod-garazh" />;
}
